server:
  port: 8081
spring:
  datasource:
    url: jdbc:tc:postgresql:17:///beatgear
    username: user
    password: password
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost:8080/realms/bg-test-realm"
          jwk-set-uri: "http://localhost:8080/realms/bg-test-realm/protocol/openid-connect/certs"
